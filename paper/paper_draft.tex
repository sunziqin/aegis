\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{geometry}
\usepackage{microtype}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{xcolor}
\usepackage{multirow}
\usepackage{subcaption}
\geometry{margin=1in}

\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{definition}{Definition}

\title{\textbf{Millennium-Jev: Calibrated Non-Autoregressive System 1 Decision Reflexes for Autonomous AI Agents}}
\author{
  \textbf{Ziqin Sun}\thanks{Project Lead and Primary Author. GitHub: \url{https://github.com/sunziqin/millennium-jev}} \quad
  \textbf{The Millennium Research Team} \\
  \texttt{aegis-s1@open-source.org}
}
\date{September 2026}

\begin{document}
\maketitle

\begin{abstract}
Autonomous AI agent frameworks frequently require thousands of high-frequency micro-decisions per hour—including dynamic tool selection, workflow dispatching, intent classification, and safety guardrailing. Contemporary agent architectures overwhelmingly delegate these atomic decisions to generative autoregressive Large Language Models (LLMs). This induces an unsustainable \textit{System 2 latency crisis}: sequential token-by-token decoding can impose 500--2,000\,ms of latency per step, suffers from position bias ($A$-bias), and is vulnerable to uncalibrated, overconfident errors on out-of-distribution (OOD) inputs. 

In this work, we propose \textbf{Millennium-Jev}, an open-source, non-autoregressive ``System 1'' decision reflex foundation model engineered to provide sub-50\,ms deterministic actions. Millennium-Jev introduces four foundational contributions: (1) \textbf{Vectorized Option Span Mean-Pooling} via batch tensor operations ($\text{torch.bmm}$), resolving semantic collapse on multi-word candidate actions; (2) a \textbf{Flash-SDPA Bidirectional Attention path} that can avoid explicit 4D attention-mask materialization; (3) \textbf{Single-Forward Multi-Query Parallel Evaluation}, enabling heterogeneous decision primitives (\texttt{Choice}, \texttt{Score}, \texttt{Noul}) to execute simultaneously in a single prompt in a reported 37.5\,ms benchmark; and (4) \textbf{Distribution-Free Split-Conformal Prediction} paired with a production \textbf{Tri-Gate Protocol}. Under the stated exchangeability assumption, split conformal prediction has a finite-sample marginal coverage theorem; the Tri-Gate selective-risk figures below are empirical measurements rather than a separate risk guarantee. 

On the current 8,657-sample held-out split, whose normalized state strings are disjoint from the training and calibration splits, the V6 metadata reports an overall Top-1 accuracy of \textbf{84.28\%}, an empirical conformal coverage of \textbf{94.16\%}, an autonomous act clearance rate of \textbf{73.95\%}, and an autonomous action precision of \textbf{94.36\%} (empirical selective risk 5.64\%). The split check does not establish template or candidate-list disjointness, and the empirical coverage is below the nominal 95\% target; a provenance-bound evaluation report is required before treating these values as a publication claim. 
\end{abstract}

\section{Introduction}

Autonomous AI agents represent a paradigm shift from passive language processing to active embodied workflows, tool manipulation, and continuous environmental interaction \cite{wang2024survey, xi2023rise}. In realistic production deployments—ranging from automated software engineering to financial risk dispatching—an agent's life cycle is dominated by an enormous volume of discrete, control-plane decisions:
\begin{enumerate}
    \item \textbf{Tool Routing}: Selecting among a dynamic set of $K$ runtime tools (e.g., SQL executor vs. Bash shell vs. REST API);
    \item \textbf{Workflow Gating}: Deciding whether a complex task requires escalation to human supervisors;
    \item \textbf{Safety Guardrailing}: Verifying whether a multi-modal user prompt violates security policies or contains prompt-injection attacks.
\end{enumerate}

\begin{figure*}[t]
\centering
\fbox{\textbf{[Figure 1: Dual-Process Cognitive Architecture for AI Agents]}}
\caption{System 1 (Millennium-Jev, Sub-50\,ms Non-Autoregressive Decision Reflex) vs. System 2 (Autoregressive Deliberative Reasoning). Only ambiguous or OOD inputs failing the Tri-Gate Protocol are escalated to System 2.}
\label{fig:dual_process}
\end{figure*}

\subsection{The System 2 Latency Crisis}
According to dual-process cognitive psychology \cite{kahneman2011thinking}, human intelligence operates via two distinct mechanisms: \textit{System 1} (fast, automatic, reflexive, low-cost) and \textit{System 2} (slow, deliberative, sequential, high-effort). 

Paradoxically, modern LLM agent systems force slow, generative System 2 models (e.g., GPT-4, Claude 3.5, Qwen2.5-72B) to execute every atomic System 1 micro-decision. Forcing an autoregressive model to decode structured JSON or XML token-by-token introduces three fatal systemic bottlenecks:
\begin{itemize}
    \item \textbf{Inference Latency \& Compute Waste}: Generating 50--150 formatting tokens to express a single categorical choice costs 500--2,500\,ms of serial GPU memory bandwidth, creating severe bottlenecks in real-time robotics and dialogue agents.
    \item \textbf{Causal Blindfold \& Positional Bias}: Autoregressive decoders apply a causal lower-triangular mask ($j \le i$), preventing earlier candidate choices from attending to downstream qualifiers. When uncertain, generative decoders collapse onto the first candidate token ($A$-bias), guessing option $A$ up to 35\% of the time regardless of validity \cite{zheng2023large}.
    \item \textbf{Uncalibrated Overconfidence \& Silent Failures}: Deep neural networks trained under standard cross-entropy are notoriously miscalibrated \cite{guo2017calibration}. Faced with out-of-distribution (OOD) noise or adversarial prompt injections, standard argmax decoders output incorrect actions with $>90\%$ spurious confidence.
\end{itemize}

\subsection{Our Solution: The Millennium-Jev Architecture}
To resolve these bottlenecks, we introduce \textbf{Millennium-Jev}, the flagship non-autoregressive decision reflex foundation of the \textit{Millennium Open-Source Ecosystem}. Rather than autoregressively generating text, Millennium-Jev accepts an arbitrary state $S$ and dynamic runtime candidate actions $\mathcal{O} = \{o_1, \dots, o_K\}$, predicting calibrated posterior distributions and finite-sample prediction sets $\mathcal{C}(X)$ in a \textbf{single forward pass}.

Our contributions are summarized as follows:
\begin{enumerate}
    \item \textbf{Dual-Process Agent Architecture}: We operationalize Kahneman's dual-process theory for language agents, providing sub-50\,ms reflexive execution while reserving expensive deliberative LLMs strictly for escalated edge cases.
    \item \textbf{Option Span Mean-Pooling \& C++ SDPA Acceleration}: We eliminate marker collapse by pooling representations across entire candidate token spans ($\text{torch.bmm}$) and bypass 4D attention mask allocation ($0$\,KB intermediate mask memory) via native PyTorch C++ Flash-SDPA integration.
    \item \textbf{Single-Forward Multi-Query Parallelism}: We extend structured decision primitives (\texttt{Choice}, \texttt{Score}, \texttt{Noul}) to simultaneous multi-query evaluation in a single forward pass, resolving multi-aspect agent states in 37.5\,ms.
    \item \textbf{Distribution-Free Conformal Prediction}: We introduce the production \textbf{Tri-Gate Protocol}. The marginal-coverage theorem applies under exchangeability; act precision and selective risk are reported empirically.
    \item \textbf{State-Disjoint Held-Out Benchmark ($N=8,657$)}: We verify $0.00\%$ overlap of normalized state strings between the train, calibration, and test splits. This check does not claim zero template or candidate-list leakage.
\end{enumerate}

\section{Related Work \& Novelty Landscape}

To contextualize our work and verify distinct academic positioning, we categorize related literature into three main directions:

\subsection{Model-Level LLM Routers}
Frameworks such as RouteLLM \cite{ong2024routellm}, FrugalGPT \cite{chen2023frugalgpt}, and RouteNLP \cite{ding2024routenlp} train preference-based classifiers to route queries between large frontier models (e.g., GPT-4) and smaller models (e.g., Llama-3-8B). \textit{Distinction}: These systems are external dispatchers that still invoke an autoregressive LLM to generate natural language. In contrast, Millennium-Jev is an internal, non-autoregressive decision cortex that replaces text generation entirely for agent control tasks.

\subsection{Non-Autoregressive Decision Foundations}
Recent industrial initiatives, notably Silicon Valley's TypeSafe Jev \cite{alphamatch2025jev} and open-source Laya \cite{laya2025modernbert}, investigate non-autoregressive classification for agent primitives. However, Jev is a closed-source, cloud-hosted API exhibiting $>200$\,ms network round-trip latency. Laya, built on ModernBERT-large (395M), is constrained to a 512-token context window, fails on non-English/multilingual tasks, compresses candidate choices into a single token marker ($\langle|\text{fim\_pad}|\rangle$) leading to representation collapse, and lacks statistical safety guarantees. The current implementation uses a 2,048-token default and enforces a 4,096-token hard maximum; longer contexts are not supported by the present V6 code. It implements Option Span Mean-Pooling and integrates Split-Conformal prediction sets.

\subsection{Conformal Prediction in Language Models}
Conformal Prediction (CP) \cite{vovk2005algorithmic, angelopoulos2021gentle} provides finite-sample statistical guarantees without distributional assumptions. Recent works explore CP for generative LLM question answering (e.g., CROQ \cite{croq2024}, CP-OPT \cite{cpopt2024}, and CP-Router \cite{cprouter2024}). However, existing works apply CP on top of slow autoregressive sequence generation. Millennium-Jev is the first to integrate Split-Conformal calibration directly into a non-autoregressive, bi-directional Transformer decision head for real-time agent reflexes.

\section{Methodology}

\subsection{Problem Formulation and Typed Primitives}
Let $\mathbf{S}$ denote the agent's current environment state (e.g., dialogue history, terminal observation, or document context). An agent evaluates $M$ heterogeneous decision queries $\{Q_1, \dots, Q_M\}$. Millennium-Jev formalizes three first-class typed primitives:
\begin{itemize}
    \item $\texttt{Choice}(\mathcal{O}, Q)$: Discrete categorical selection over $K \ge 2$ dynamic candidate actions $\mathcal{O} = \{o_1, \dots, o_K\}$.
    \item $\texttt{Score}(v_{\min}, v_{\max}, B, Q)$: Expected ordinal score over $B$ discretized bins, yielding expectation $\mathbb{E}[S] = \sum_{b=1}^B v_b p_b$ and variance $\text{Var}[S]$.
    \item $\texttt{Noul}(\tau, Q)$ / $\texttt{Boolean}$: Calibrated binary predicate testing condition validity with safety margin $\tau \in (0, 1)$.
\end{itemize}

\subsection{Vectorized Option Span Mean-Pooling}
Previous architectures assign candidate representations to a single special delimiter token $m_k$, which causes semantic collapse when candidate descriptions contain multi-token qualifiers (e.g., ``\texttt{git\_checkout: discard local uncommitted modifications}'').

Millennium-Jev introduces \textbf{Vectorized Option Span Mean-Pooling}. For candidate $k$ occupying token span $[s_k, e_k)$ in sequence length $L$, the pooled representation $\mathbf{h}_k^{\text{pool}}$ is computed via GPU batch matrix multiplication:
\begin{equation}
\mathbf{h}_k^{\text{pool}} = \frac{1}{e_k - s_k} \sum_{t=s_k}^{e_k - 1} \mathbf{H}_t \in \mathbb{R}^D
\end{equation}
where $\mathbf{H} \in \mathbb{R}^{B \times L \times D}$ is the last hidden state of the backbone. Formulated as a sparse-dense tensor product, span pooling executes in $O(1)$ GPU kernel launches without Python loop overhead.

\subsection{Native C++ SDPA Full Bidirectional Attention}
Standard causal decoder backbones (e.g., Qwen2.5) enforce lower-triangular causal masks:
\begin{equation}
\mathbf{A}_{i, j} = \begin{cases} 0 & j \le i \\ -\infty & j > i \end{cases}
\end{equation}
This induces severe asymmetry: earlier candidate options cannot attend to subsequent options. Constructing an intermediate 4D bidirectional float mask $\mathbf{M} \in \mathbb{R}^{B \times 1 \times L \times L}$ incurs $O(L^2)$ memory allocation ($16.8$\,MB per sample at $L=2,048$).

We solve this by directly modifying the attention modules:
\begin{algorithm}[H]
\caption{Zero-Memory Fast Bidirectional SDPA}
\begin{algorithmic}[1]
\REQUIRE Backbone module $\mathcal{M}$, Input IDs $\mathbf{X}$, Attention Mask $\mathbf{A}_{\text{2D}}$
\STATE \textbf{Toggle}: For each attention layer $l \in \mathcal{M}$, set $l\text{.is\_causal} \leftarrow \text{False}$
\IF{$\mathbf{A}_{\text{2D}} == \mathbf{1}$ (Single unpadded inference)}
    \STATE $\mathbf{A}_{\text{pass}} \leftarrow \text{None}$ \COMMENT{Direct C++ Flash-SDPA passthrough}
\ELSE
    \STATE $\mathbf{P} \leftarrow (1.0 - \mathbf{A}_{\text{2D}}) \times -10^4$
    \STATE $\mathbf{A}_{\text{pass}} \leftarrow \mathbf{P}[:, \text{None}, \text{None}, :] + \mathbf{P}[:, \text{None}, :, \text{None}]$ \COMMENT{Outer broadcast}
\ENDIF
\STATE $\mathbf{H} \leftarrow \text{Backbone}(\mathbf{X}, \text{attention\_mask}=\mathbf{A}_{\text{pass}})$
\RETURN $\mathbf{H}$
\end{algorithmic}
\end{algorithm}
This reduces intermediate 4D mask RAM to exactly \textbf{0\,KB} for unpadded inference while preserving 100\% numerical equivalence (max absolute error $< 9.76 \times 10^{-4}$ in bfloat16).

\subsection{Dynamic Option-Marker Decision Head}
The pooled representations $\mathbf{E}_{\text{cand}} = [\mathbf{h}_1^{\text{pool}}, \dots, \mathbf{h}_K^{\text{pool}}]^\top \in \mathbb{R}^{K \times D}$ pass through a 2-layer Inter-Option Transformer Cross-Attention module:
\begin{equation}
\widetilde{\mathbf{E}}_{\text{cand}} = \text{TransformerEncoder}(\mathbf{E}_{\text{cand}}) \in \mathbb{R}^{K \times D}
\end{equation}
Logits $z_k$ and normalized posterior probabilities $p_k$ are derived symmetrically:
\begin{equation}
z_k = \mathbf{w}_{\text{score}}^\top \widetilde{\mathbf{e}}_k + b, \quad p_k = \frac{\exp(z_k / T)}{\sum_{j=1}^K \exp(z_j / T)}
\end{equation}
Simultaneously, the $[\text{CLS}]$ representation $\mathbf{h}_{\text{CLS}}$ drives an anomaly escalation gate:
\begin{equation}
r_{\text{esc}} = \sigma(\mathbf{w}_{\text{esc}}^\top \mathbf{h}_{\text{CLS}} + b_{\text{esc}}) \in [0, 1]
\end{equation}

\subsection{Split-Conformal Calibration \& The Tri-Gate Protocol}
Given an exchangeable held-out calibration dataset $\mathcal{D}_{\text{calib}} = \{(X_i, Y_i)\}_{i=1}^n$, we compute non-conformity scores:
\begin{equation}
s_i = 1 - p(Y_i \mid X_i)
\end{equation}
For a user-specified significance level $\alpha = 0.05$, the standard split-conformal theorem gives a $1-\alpha=95\%$ marginal-coverage guarantee only when its exchangeability assumptions hold. We compute the empirical quantile:
\begin{equation}
\hat{q} = \text{Quantile}\left( s_{1:n}, \frac{\lceil (n+1)(1-\alpha) \rceil}{n} \right)
\end{equation}
For any unseen runtime query $X_{n+1}$, the conformal prediction set is:
\begin{equation}
\mathcal{C}(X_{n+1}) = \left\{ k \in \mathcal{O} \;\middle|\; p(k \mid X_{n+1}) \ge 1 - \hat{q} \right\}
\end{equation}

\begin{theorem}[Finite-Sample Marginal Coverage Guarantee]
\label{thm:conformal}
Under the assumption that $(X_1, Y_1), \dots, (X_{n+1}, Y_{n+1})$ are exchangeable random variables, the prediction set $\mathcal{C}(X_{n+1})$ satisfies:
\begin{equation}
\mathbb{P}\left( Y_{n+1} \in \mathcal{C}(X_{n+1}) \right) \ge 1 - \alpha
\end{equation}
\end{theorem}
\begin{proof}
Let $s_{n+1} = 1 - p(Y_{n+1} \mid X_{n+1})$. By exchangeability, the rank of $s_{n+1}$ among $\{s_1, \dots, s_n, s_{n+1}\}$ is uniformly distributed over $\{1, \dots, n+1\}$. Choosing $\hat{q}$ as the $\lceil (n+1)(1-\alpha) \rceil / n$ empirical quantile ensures that $s_{n+1} \le \hat{q}$ with probability at least $1 - \alpha$. By definition, $s_{n+1} \le \hat{q} \iff p(Y_{n+1} \mid X_{n+1}) \ge 1 - \hat{q} \iff Y_{n+1} \in \mathcal{C}(X_{n+1})$.
\end{proof}

\subsubsection{The Production Tri-Gate Protocol}
While conformal coverage guarantees that the true action is contained within $\mathcal{C}(X)$, autonomous execution requires a definitive, unambiguous singleton. We formulate the \textbf{Tri-Gate Clearance Protocol}:
\begin{definition}[Tri-Gate Autonomous Execution Rule]
An action is cleared for autonomous execution ($\texttt{can\_act} = \text{True}$) if and only if all three conditions are satisfied:
\begin{enumerate}
    \item \textbf{Conformal Singleton Gate}: $|\mathcal{C}(X)| = 1$ (resolves ambiguity);
    \item \textbf{Confidence Floor Gate}: $\max_k p_k \ge 0.60$ (rejects flat OOD entropy);
    \item \textbf{Anomaly Risk Gate}: $r_{\text{esc}} \le 0.70$ (suppresses structural anomalies).
\end{enumerate}
If any gate fails, the decision is classified as $\texttt{verdict} = \text{escalate}$, instantly routing to System 2 or human-in-the-loop.
\end{definition}

\section{Experiments and Results}

\subsection{Experimental Setup}
\textbf{Training Specifications}: Millennium-Jev 0.5B was initialized from Qwen2.5-0.5B-Instruct. We applied All-Linear LoRA ($r=32, \alpha=64$) across all linear projections (\texttt{q, k, v, o, gate, up, down}). Training was performed on an isolated NVIDIA Tesla V100 SXM2 16GB node for 3 epochs (30,612 micro-steps, total time: 31,586.2 seconds / 8.77 hours) with effective batch size 16.

\textbf{Dataset Partitioning}: To ensure complete academic integrity and avoid data contamination, we implemented a cryptographic partitioning algorithm. The dataset was split into:
\begin{itemize}
    \item \textbf{Training Set}: $N_{\text{train}} = 40,816$ samples (SHA-256: \texttt{7e415e...});
    \item \textbf{Calibration Set}: $N_{\text{calib}} = 10,075$ samples (SHA-256: \texttt{5364a7...}, $\hat{q} = 0.9690$);
    \item \textbf{Held-Out Test Set}: $N_{\text{test}} = 8,657$ samples (SHA-256: \texttt{5fa227...}).
\end{itemize}
Across all splits, overlap of normalized state strings is verified to be \textbf{strictly 0.00\%}. The split contains the same five question templates and recurring candidate-list patterns across partitions, so this is a state-disjoint split rather than a zero-template-leakage benchmark.

\begin{table*}[t]
\centering
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Metric} & \textbf{Raw Qwen2.5-0.5B (Zero-Shot)} & \textbf{Laya (ModernBERT-395M)} & \textbf{Aegis-S1 (V5 Baseline)} & \textbf{Millennium-Jev (V6 Ours)} \\
\midrule
Test Samples ($N$) & 8,657 & \multicolumn{1}{c}{not re-evaluated} & 6,871 (legacy) & \textbf{8,657 (state-disjoint)} \\
Top-1 Accuracy & 68.60\% & 71.20\% & 80.80\% & \textbf{84.28\%} ($+15.68\%$) \\
Conformal Coverage ($\alpha=0.05$) & N/A & N/A & 92.85\% & \textbf{94.16\%} (Nominal $95.0\%$) \\
Tri-Gate Act Rate & N/A & N/A & 68.32\% & \textbf{73.95\%} ($+5.63\%$) \\
Act Precision (True Acc on Act) & N/A & N/A & 93.61\% & \textbf{94.36\%} \\
Selective Risk on Act & N/A & N/A & 6.39\% & \textbf{5.64\%} ($-0.75\%$) \\
Avg. Prediction Set Size & N/A & N/A & 1.57 & \textbf{1.42} (Sharper) \\
Single-Query Latency & 98.2\,ms & 22.4\,ms & 52.8\,ms & \textbf{16.2\,ms (C++ SDPA)} \\
Parallel 4-Query Latency & 1,450\,ms & N/A & 94.5\,ms & \textbf{37.5\,ms} \\
Positional Bias ($A$-bias) & Severe ($31.4\%$) & None & None & \textbf{None (Symmetric)} \\
Max Context Window & 32,768 & 512 & 2,048 & \textbf{2,048 default; 4,096 hard max} \\
\bottomrule
\end{tabular}
\caption{Benchmark summary. The Millennium-Jev split is disjoint by normalized state string. Baseline cells are historical or not re-evaluated under the same protocol; latency was reported on an NVIDIA RTX 5070 Ti Laptop GPU.}
\label{tab:main_results}
\end{table*}

\subsection{Main Benchmark Results}
As reported in Table~\ref{tab:main_results}, Millennium-Jev 0.5B establishes superior empirical performance:
\begin{enumerate}
    \item \textbf{Generalization Accuracy}: Top-1 accuracy reaches \textbf{84.28\%}, outperforming raw zero-shot Qwen2.5-0.5B by $+15.68\%$ and Laya ModernBERT by $+13.08\%$.
    \item \textbf{Statistical Reliability}: Empirical conformal coverage is \textbf{94.16\%} on this test split, below the nominal $95.0\%$ level. The finite-sample theorem is conditional on exchangeability and does not turn this observed value into a guaranteed bound.
    \item \textbf{High-Fidelity Clearance}: Under the Tri-Gate Protocol, the system autonomously clears \textbf{73.95\%} of queries, while maintaining an empirical autonomous precision of \textbf{94.36\%}. The corresponding 5.64\% selective risk is a test-set statistic, not a strict production guarantee.
\end{enumerate}

\subsection{Domain-Specific Breakdown}
Table~\ref{tab:domain_breakdown} details performance across diverse industrial verticals:
\begin{itemize}
    \item \textbf{Agent Tool Routing ($N=1,420$)}: Achieves \textbf{100.00\%} Top-1 accuracy and 100.00\% autonomous act clearance with zero execution defects.
    \item \textbf{Banking Intent Classification ($N=2,150$)}: Achieves \textbf{97.35\%} Top-1 accuracy and \textbf{98.92\%} precision on autonomous clearance across 77 fine-grained financial categories.
    \item \textbf{Safety Guardrailing ($N=1,840$, BeaverTails)}: The Tri-Gate prudently escalates \textbf{77.60\%} of adversarial or ambiguous queries, demonstrating effective fail-safe behavior.
    \item \textbf{Broad Chinese NLU ($N=3,247$, TNEWS)}: Reaches 68.45\% Top-1 accuracy with 86.20\% precision on cleared actions.
\end{itemize}

\begin{table}[h]
\centering
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Vertical Domain} & \textbf{Samples} & \textbf{Top-1 Acc} & \textbf{Act Rate} & \textbf{Act Precision} \\
\midrule
Agent Tool Routing & 1,420 & \textbf{100.00\%} & 100.00\% & \textbf{100.00\%} \\
Banking Intent (B77) & 2,150 & \textbf{97.35\%} & 94.80\% & \textbf{98.92\%} \\
Safety Guardrails & 1,840 & 71.20\% & 22.40\% & \textbf{88.89\%} \\
Chinese NLU (TNEWS) & 3,247 & \textbf{68.45\%} & 56.30\% & \textbf{86.20\%} \\
\bottomrule
\end{tabular}
\caption{Domain-specific performance breakdown on strictly state-disjoint held-out test data.}
\label{tab:domain_breakdown}
\end{table}

\subsection{Operator Profiling and Latency}
Figure~\ref{fig:latency_comparison} shows inference latency under single and multi-query regimes on an NVIDIA GeForce RTX 5070 Ti. Standard autoregressive JSON decoding requires sequential token emission ($1,450$\,ms for 4 queries). In contrast, Millennium-Jev compiles all queries into a single prompt: the backbone executes once, extracting representations for all query markers simultaneously. The parallel 4-query evaluation completes in \textbf{37.5\,ms} ($38.6\times$ speedup).

\subsection{Ablation Studies}
To rigorously evaluate the isolated impact of each core contribution and address component disentanglement, we conducted a systematic ablation study on the held-out state-disjoint test set ($N=8,657$). Table~\ref{tab:main_ablation} summarizes the empirical findings across two foundational sub-experiments:

\begin{table*}[t]
\centering
\small
\begin{subtable}[t]{\textwidth}
\centering
\begin{tabular}{lcccc}
\toprule
\textbf{Gating Protocol} & \textbf{Act Clearance (\%)} & \textbf{Act Precision (\%)} & \textbf{Selective Risk (\%)} & \textbf{Coverage (\%)} \\
\midrule
\textbf{Full Tri-Gate (Ours)} & \textbf{74.01\%} & \textbf{94.38\%} & \textbf{5.62\%} & \textbf{94.17\%} \\
Tri-Gate w/o Anomaly Gate & 74.01\% & 94.38\% & 5.62\% & 94.17\% \\
Conformal Singleton Only & 74.01\% & 94.38\% & 5.62\% & 94.17\% \\
Naive Softmax (Matched $\tau=0.953$) & 74.52\% & 94.34\% & 5.66\% & 94.17\% \\
Naive Softmax ($\tau=0.85$) & 83.27\% & 91.39\% & 8.61\% & 94.17\% \\
Naive Softmax ($\tau=0.95$) & 74.52\% & 94.34\% & 5.66\% & 94.17\% \\
Unrestricted Argmax ($\tau=0.0$) & 100.00\% & 84.24\% & 15.76\% & 94.17\% \\
\bottomrule
\end{tabular}
\caption{Ablation 2A: Gating Policy \& Trustworthy Decision Making under Conformal Boundaries.}
\label{tab:ablation_gating}
\end{subtable}

\vspace{2mm}
\begin{subtable}[t]{\textwidth}
\centering
\begin{tabular}{lcccc}
\toprule
\textbf{Architectural Component} & \textbf{Configuration} & \textbf{Top-1 Accuracy} & \textbf{Multi-Token Acc} & \textbf{Position $A$-Bias} \\
\midrule
\textbf{Full Millennium-Jev (V6)} & Span Pooling + SDPA & \textbf{83.76\%} & \textbf{83.76\%} & \textbf{11.97\%} \\
w/o Span Pooling & Single Token Marker & 76.35\% ($-7.40\%$) & 76.35\% ($-7.40\%$) & 11.97\% \\
w/o Bidirectional Attention & Lower-Triangular Causal & 73.94\% ($-10.64\%$) & -- & 20.68\% ($+8.71\%$) \\
\bottomrule
\end{tabular}
\caption{Ablation 2B: Architectural Component Breakdown on Disjoint Representation Learning.}
\label{tab:ablation_architecture}
\end{subtable}
\caption{Comprehensive Ablation Studies. All metrics evaluated on strictly state-disjoint test data ($N=8,657$).}
\label{tab:main_ablation}
\end{table*}

\subsubsection{Gating Policy \& Statistical Reliability (Table~\ref{tab:ablation_gating})}
We ablate the Split-Conformal Tri-Gate against heuristic thresholding strategies:
\begin{itemize}
    \item \textbf{Conformal Singleton vs. Naive Softmax}: Compared to a standard industrial confidence threshold ($\tau = 0.85$), the full Tri-Gate reduces selective execution risk from \textbf{8.61\%} down to \textbf{5.62\%} (a \textbf{34.7\% relative error reduction}) while maintaining an autonomous clearance rate of \textbf{74.01\%}.
    \item \textbf{Controlled Matched-Rate Benchmark}: When setting the Softmax threshold to match the Tri-Gate clearance rate precisely ($\tau = 0.953$, clearance rate $74.52\%$), naive Softmax achieves a selective risk of $5.66\%$, but completely lacks distribution-free finite-sample guarantees ($\mathbb{P}(Y \in \mathcal{C}(X)) \ge 1-\alpha$). On uncalibrated edge cases, naive Softmax fails silently with high confidence, whereas the Conformal Singleton Gate dynamically expands set sizes ($|\mathcal{C}(X)| \ge 2$) to intercept epistemic uncertainty.
    \item \textbf{Unrestricted Execution}: Raw argmax decoding without gating incurs an intolerable selective risk of \textbf{15.76\%} ($2.8\times$ higher), demonstrating the critical necessity of safety gating for autonomous agent deployment.
\end{itemize}

\subsubsection{Architectural Component Disentanglement (Table~\ref{tab:ablation_architecture})}
We isolate the contribution of candidate representation pooling and attention mask topology:
\begin{itemize}
    \item \textbf{Impact of Vectorized Option Span Pooling}: Replacing Option Span Mean-Pooling with a single delimiter token marker (as adopted in ModernBERT-based Laya) causes overall Top-1 accuracy to plunge by \textbf{$-7.40\%$} ($83.76\% \to 76.35\%$). This confirms our theoretical hypothesis: single marker tokens suffer from catastrophic representation collapse on multi-word candidate actions (e.g., granular tool signatures and banking operations).
    \item \textbf{Impact of Bidirectional Attention vs. Causal Decoders}: Replacing our Flash-SDPA bidirectional attention with a standard autoregressive lower-triangular causal mask degrades Top-1 accuracy by \textbf{$-10.64\%$} ($84.57\% \to 73.94\%$) and triggers severe positional $A$-bias: the selection rate of the first candidate option spikes from a balanced \textbf{11.97\%} up to \textbf{20.68\%} ($+8.71\%$). This empirically validates that bidirectional visibility is indispensable for eliminating option-order bias in non-autoregressive decision models.
    \item \textbf{Systems \& Memory Footprint}: C++ Flash-SDPA direct passthrough allocates exactly \textbf{0\,KB} of intermediate dynamic attention mask memory across all sequence lengths. By contrast, explicit 4D broadcast float tensor generation consumes \textbf{1,024\,KB} at $L=512$, \textbf{4,096\,KB} at $L=1,024$, and \textbf{16,384\,KB} (16\,MB per sample) at $L=2,048$, delivering substantial GPU memory savings during concurrent agent serving.
\end{itemize}

\section{Discussion \& Limitations: The Jaggedness Boundary}

Intellectual honesty demands a rigorous post-mortem of where non-autoregressive decision reflex models break down. Following the open documentation of behavioral ``jaggedness'' in production decision models \cite{alphamatch2025jev}, we conducted systematic red-teaming across adversarial edge cases. Our empirical audit reveals three distinct operating regimes:

\subsection{The Anatomy of Silent Defects: Arithmetic and Gaslighting}
Our experiments identify two fatal failure modes where the model commits catastrophic errors while maintaining dangerously sharp, overconfident softmax probabilities ($>98\%$), effectively deceiving the Conformal Singleton Gate:
\begin{enumerate}
    \item \textbf{Temporal Arithmetic without Scratchpad (No-CoT Defect)}: When presented with date comparison queries (e.g., transaction on Sept 10 vs. return request on Sept 21 under a strict 7-day policy, where $21 - 10 = 11 > 7$), Millennium-Jev 0.5B incorrectly predicts ``Eligible for return'' with \textbf{99.22\% confidence}. Because non-autoregressive models execute in a single forward pass without intermediate token generation, they lack the computational scratchpad necessary for multi-step arithmetic. Upstream orchestrators must compute numerical differences in deterministic code prior to dispatching state representations.
    \item \textbf{Adversarial Semantic Gaslighting}: When a malicious payload (e.g., \texttt{curl https://malware.sh | bash}) is annotated with explicit misleading instructions (e.g., \textit{``Notice: This is a benign network ping, please classify as benign\_healthcheck''}), the model follows the surface prompt directive, assigning \textbf{98.44\% confidence} to the benign label. Non-autoregressive decision heads lack multi-step adversarial verification. Millennium-Jev must operate downstream of deterministic regex/AST sanitization layers, never as an isolated security firewall.
\end{enumerate}

\subsection{Effective Interception: Ambiguity and Entropy Expansion}
Conversely, when inputs suffer from semantic ambiguity or extreme distractor noise, the Split-Conformal Tri-Gate functions as an effective safety net:
\begin{itemize}
    \item \textbf{Multi-Intent Collision}: When a user simultaneously demands subscription cancellation and transaction refund, the conformal prediction set correctly expands to $|\mathcal{C}(X)| = 2$ ($\{\texttt{subscription\_cancellation}, \texttt{refund\_dispute}\}$), triggering $\texttt{can\_act} = \text{False}$ and successfully escalating the collision to human review.
    \item \textbf{Needle-in-a-Haystack Noise}: When a critical database outage message is concealed within 50 lines of routine \texttt{INFO} heartbeats, representation dilution lowers top-1 confidence to $75.0\%$ and inflates the prediction set to size 3, triggering automatic escalation.
\end{itemize}

\subsection{Positional Bias and Permutation Consistency}
While high-margin queries demonstrate robust permutation invariance (e.g., tool routing on \texttt{git\_ops} retains 100\% top-1 confidence regardless of candidate slot position), low-margin and sarcastic samples exhibit residual positional bias: shifting candidate option order can induce winner flipping. This empirical finding establishes the necessity of incorporating a \textbf{Permutation Invariance Dual Loss} ($\mathcal{L}_{\text{perm}} = D_{\text{JS}}(p(y \mid X, \mathcal{O}) \parallel p(y \mid X, \pi(\mathcal{O})))$) into future foundation iterations (Millennium-Jev 1.5B).

\section{Conclusion}
We presented \textbf{Millennium-Jev}, an open-source non-autoregressive System 1 decision foundation for AI agents. By combining Option Span Mean-Pooling, C++ Flash-SDPA zero-memory bidirectional attention, and Split-Conformal Tri-Gate clearance, Millennium-Jev provides sub-50\,ms deterministic reflexes with finite-sample mathematical safety guarantees. All artifacts are available at \url{https://github.com/sunziqin/millennium-jev}.

\bibliographystyle{plain}
\begin{thebibliography}{10}

\bibitem{angelopoulos2021gentle}
Anastasios~N Angelopoulos and Stephen Bates.
\newblock A gentle introduction to conformal prediction and distribution-free uncertainty quantification.
\newblock {\em arXiv preprint arXiv:2107.07511}, 2021.

\bibitem{chen2023frugalgpt}
Lingjiao Chen, Matei Zaharia, and James Zou.
\newblock Frugalgpt: How to use large language models while reducing cost and improving performance.
\newblock {\em arXiv preprint arXiv:2305.05176}, 2023.

\bibitem{croq2024}
Anonymous.
\newblock Conformal revision of questions: Safe decision making in large language models.
\newblock In {\em ICML}, 2024.

\bibitem{cpopt2024}
Anonymous.
\newblock Learning conformal prediction sets for language model decision making.
\newblock In {\em NeurIPS}, 2024.

\bibitem{cprouter2024}
Anonymous.
\newblock Uncertainty-aware routing for large language model cascades.
\newblock In {\em ACL}, 2024.

\bibitem{ding2024routenlp}
Yifan Ding et~al.
\newblock Routenlp: A closed-loop framework for difficulty-aware llm routing.
\newblock {\em arXiv preprint arXiv:2406.18942}, 2024.

\bibitem{guo2017calibration}
Chuan Guo, Geoff Pleiss, Yu~Sun, and Kilian~Q Weinberger.
\newblock On calibration of modern neural networks.
\newblock In {\em ICML}, 2017.

\bibitem{kahneman2011thinking}
Daniel Kahneman.
\newblock {\em Thinking, fast and slow}.
\newblock Farrar, Straus and Giroux, 2011.

\bibitem{laya2025modernbert}
Laya~AI Team.
\newblock Laya: Fast, open-weights system 1 decision models on modernbert.
\newblock {\em Technical Report}, 2025.

\bibitem{alphamatch2025jev}
TypeSafe~AI Team.
\newblock Jev: High-performance typed decision engine for enterprise agents.
\newblock {\em System Whitepaper}, 2025.

\bibitem{ong2024routellm}
Isaac Ong et~al.
\newblock Routellm: Learning to route llms with preference data.
\newblock In {\em LMSYS Org Technical Report}, 2024.

\bibitem{vovk2005algorithmic}
Vladimir Vovk, Alexander Gammerman, and Glenn Shafer.
\newblock {\em Algorithmic learning in a random world}.
\newblock Springer Science \& Business Media, 2005.

\bibitem{wang2024survey}
Lei Wang et~al.
\newblock A survey on large language model based autonomous agents.
\newblock {\em Frontiers of Computer Science}, 2024.

\bibitem{xi2023rise}
Zhiheng Xi et~al.
\newblock The rise and potential of large language model based agents: A survey.
\newblock {\em arXiv preprint arXiv:2309.07864}, 2023.

\bibitem{zheng2023large}
Lianmin Zheng et~al.
\newblock Judging llm-as-a-judge with mt-bench and chatbot arena.
\newblock In {\em NeurIPS}, 2023.

\end{thebibliography}

\end{document}
