\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb,amsfonts,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{geometry}
\geometry{margin=1in}

\title{\textbf{Aegis-S1: Provably Safe Non-Autoregressive System-1 Decision Models for LLM Agents}}
\author{Anonymous Authors}
\date{September 2026}

\begin{document}
\maketitle

\begin{abstract}
Autonomous Large Language Model (LLM) agents frequently execute atomic, high-frequency control decisions—such as tool dispatching, conversational turn-taking, and safety guardrailing. Conventional LLMs formulate these decisions as autoregressive sequence generation, incurring sequential decoding cost while remaining vulnerable to formatting failures and overconfident mistakes. In this work, we propose \textbf{Aegis-S1}, an open-source, non-autoregressive ``System 1'' decision framework that eliminates token-by-token generation in favor of a single forward pass. Aegis-S1 introduces: (1) a Dynamic Option-Marker Attention Head that models mutual competition across variable candidate actions; and (2) distribution-free Conformal Prediction calibration, establishing rigorous, finite-sample statistical safety guarantees ($P(Y \in \mathcal{C}(X)) \ge 1 - \alpha$) that convert epistemic uncertainty into principled escalation rather than silent failures. The current reference implementation has a 4096-token hard input limit and $O(L^2)$ memory cost for its bidirectional 4D attention. The repository does not yet contain a regenerated, provenance-validated V6 result, so quantitative accuracy, latency, and OOD claims are intentionally omitted until the current disjoint evaluation is complete.
\end{abstract}

\section{Introduction}
Modern artificial intelligence systems increasingly operate as autonomous agents interacting with complex environments via tool execution, database queries, and external APIs. Dual-process cognitive theory posits that human cognition is divided into two synergistic systems: System 1 (rapid, intuitive reflexes) and System 2 (slow, deliberative reasoning). Current AI agent frameworks overwhelmingly rely on large generative autoregressive models (System 2, e.g., GPT-4, Claude) for every micro-decision. For instance, determining whether a customer service user has finished speaking (turn-taking) or choosing between a calculator and a search engine (tool routing) requires generating sequences token-by-token.

This architectural paradigm suffers from three critical bottlenecks: (1) Latency and Resource Inefficiency, (2) Positional Bias and Formatting Fragility, and (3) Overconfidence and Silent Failures. In this paper, we introduce \textbf{Aegis-S1}, addressing these limitations through a dynamic option-marker scoring mechanism with inter-option cross-attention. The current reference implementation is bounded at 4096 input tokens and requires sequence-length-specific memory benchmarks.

\section{Methodology}
\subsection{Dynamic Option-Marker Architecture}
Given a decision context $\mathbf{S}$ and dynamic candidate actions $\mathcal{O} = \{o_1, \dots, o_K\}$, the input sequence is formatted as:
\begin{equation}
\mathbf{X} = [\text{CLS}] \circ \mathbf{S} \circ \bigoplus_{k=1}^K \left( \tau_{\text{marker}} \circ o_k \right) \circ [\text{SEP}]
\end{equation}
Candidate marker representations $\mathbf{E}_{\text{cand}} \in \mathbb{R}^{K \times D}$ are gathered directly from token indices $m_1, \dots, m_K$.

\subsection{Inter-Option Cross-Attention}
To enforce symmetric competition and mutual exclusivity without left-to-right positional bias:
\begin{equation}
\widetilde{\mathbf{E}}_{\text{cand}} = \text{TransformerLayer}(\mathbf{E}_{\text{cand}}, \text{Mask}_{\text{cand}})
\end{equation}
\begin{equation}
p_k = \frac{\exp(z_k)}{\sum_{j=1}^K \exp(z_j)}, \quad z_k = \mathbf{w}_{\text{score}}^\top \widetilde{\mathbf{e}}_k + b
\end{equation}

\subsection{Conformal Risk Control}
Under Split Conformal Prediction, the prediction set $\mathcal{C}(X_{n+1}) = \{ k \in \mathcal{O} \mid p(k \mid X_{n+1}) \ge 1 - \hat{q} \}$ satisfies:
\begin{equation}
\mathbb{P}\left( Y_{n+1} \in \mathcal{C}(X_{n+1}) \right) \ge 1 - \alpha
\end{equation}
When $|\mathcal{C}(X)| = 1$, the system automatically acts; when $|\mathcal{C}(X)| > 1$ or $|\mathcal{C}(X)| = 0$, the system escalates or rejects.

\section{Experiments and Results}
The quantitative results table is deliberately omitted from this draft. It must be regenerated from the current checkpoint, calibration artifact, disjoint splits, dependency versions, and target hardware; historical numbers are not valid deployment evidence.

\section{Conclusion}
Aegis-S1 provides a non-autoregressive decision path and a conformal escalation mechanism. The safety guarantee applies to a valid exchangeable calibration protocol; latency, accuracy, and context claims remain empirical questions for the regenerated evaluation.

\end{document}
